// 載入 material 庫
import 'package:flutter/material.dart';

// 進入點
// runApp 是 Flutter 進入點
void main() { runApp(
  new Center(
    child: new Text(
      'First app',
      textDirection: TextDirection.ltr,
    ),
  ));
}
